name: Lint

on:
  pull_request:
    paths:
      - 'apps/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/lint.yml'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint workflow placeholder"

