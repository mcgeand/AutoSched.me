name: Test

on:
  pull_request:
    paths:
      - 'apps/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/test.yml'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test workflow placeholder"

